A simple command line for the web. It turns out it has been [done](http://yubnub.org/) [before](http://shortwaveapp.com/), but what the heck.

Currently requires a server, but may eventually be able to work as a standalone bookmarklet.